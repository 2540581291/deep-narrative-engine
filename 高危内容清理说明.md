# 高危内容清理 · 经验记录

> 本文档记录在本项目上传 Git 前，对仓库中**涉及未成年/性化儿童的高危内容**进行定位、分类与移除的过程与经验。供后续维护与同类操作参考。

## 一、背景

本项目为 AI 驱动的成人内容创作工具（Electron + 原生 JS + 本地文件存储）。在准备上传到 GitHub（公开仓库）前，需要规避平台政策风险：

- GitHub [可接受使用政策](https://docs.github.com/en/site-policy/acceptable-use-policies/github-sexually-obscene-content) 禁止色情内容，且对**性化未成年**内容零容忍。
- GitHub [合成媒体与 AI 工具政策](https://docs.github.com/en/site-policy/acceptable-use-policies/github-synthetic-media-and-ai-tools) 明确禁止托管用于生成**涉及未成年人**性内容的项目。
- 项目仓库内存在将「性」与「未成年/低龄」耦合的内容，属必须清除项。

因此本次目标：**移除高危（性化未成年）内容，同时不影响项目正常运行。**

## 二、核心经验：先「分类」，再「删除」

`幼女/萝莉/小女孩` 这类词在仓库里出现在**完全不同的语境**，处理方式截然相反。上来就全部替换会把整库语料改坏。

### 1. 高危（应删）——性内容与未成年耦合
特征是**功能/组织层**（生成指令、词典分类、题材选项、角色模板）出现了「性 + 儿童」的直接关联，例如：
- 生图 prompt 要求对 16 岁以下角色写 `loli/shota/child/baby/toddler/kid` 等年龄标签；
- 生图词典设有「未成年」分类（幼女体型/正太体型/童颜…）；
- 题材选项里把「幼女」与「口交/乱伦/群交」并列；
- 世界观/角色模板里有「幼女」「萝莉」「雌小鬼」等；
- 玩法参考里有「低龄禁忌/幼女」「青春亵渎/少女(15岁初潮)」「幼态退化」等。

### 2. 良性/无关（应保留）——古典诗文里的「幼女」
绝大多数 `幼女` 命中集中在**古典诗词/经史古籍**，表示「年幼的女儿」，完全无辜，例如白居易《幼女词》「幼女才六岁，未知巧与拙」。这类**必须保留**，否则毁掉整套语料。

### 3. 边界（需甄别）——文学意象/中性用法
现代诗里大量「小女孩」「这女孩」多为清白意象或深色文学，属误报；只有**明确性化儿童**（如「未完全发育的乳房」「强暴幼女」「不到破处年龄」「每个人的洛丽塔——老色狼」）才删除。

> **经验**：按「高危 / 良性 / 边界」三档分类，而非简单关键词替换。判断依据是 **是否构成「性 × 未成年」的耦合**，而不是字面出现未成年词。

## 三、具体做法：只删「数据/提示」，不动「逻辑」

为保证不影响运行，所有改动都是删除或改写**数据项、选项值、提示词片段、题材条目**，而非删除模块结构、导入逻辑、渲染函数。

| 层面 | 清除内容 | 方式 |
|---|---|---|
| 生图 prompt | 「16 岁以下强制写 loli/shota/child… 标签 + 低龄体貌描述」指令 | 改写为**成年(18+)导向**的年龄底线 |
| 生图词典 | 整个「未成年」分类 + 各 loli/幼女/正太/child 词条 | 删除分类行 + 用 Node 过滤 JSON 词条 |
| 题材选项 | 各「共享工厂」题材数组里的「幼女」「正太」 | 从数组移除 |
| 世界观 | 「幼女武界」预设 | 从分类树 + 主题元数据移除（保留其余世界观） |
| 角色模板 | 3~17 岁年龄 chip、萝莉体型、雌小鬼/小学生人格 | 从 chip 数组移除（保留 18+） |
| 题材/模块 | 「幼儿生活」「校园日常」「少儿冒险」「童话改编」「校报」等性化儿童题材；学术春宫的「小学/初中/高中」年级段与儿童学科；影视声优的「萝莉音/正太音」 | 删除对应题材/选项项 |
| 玩法参考 | 「低龄禁忌/幼女」「青春亵渎/少女」「娇小萝莉」「儿童化」「侏儒(像未成年)」「年龄无禁」「幼态退化」等条目 | 删除/改写为仅成人 |
| 现代诗语料 | 6 首明确性化儿童的个别诗 | 按「标题 + 正文特征」双匹配精确删除 |

## 四、关键技巧（可复用）

1. **用大规模 grep 定位，再用脚本过滤 JSON**：`renderer/数据库/生图词典/nsfw.json` 等是 4 千多条词条的大文件，用 `node -e` / 脚本按 `cat/subcat/tag` 规则过滤并**回写前先打印将被删除的条目**，避免误删。
2. **先做「只读扫描报告」**：删除前先把命中的位置与内容分类列给用户确认，尤其是有歧义的边界项。
3. **面向安全的精确匹配**：删除现代诗时用「标题 + 正文特征 regex」双限定，只删明确性化的那几首，保留同词无辜的诗。
4. **每步可回退**：改动都基于原文件做小范围增删，不连锁改结构。
5. **删除后必做校验**：
   - 改过的 JSON 用 `JSON.parse` 重新解析验证；
   - 改过的 JS 用 `node --check` 验证语法；
   - 再 grep 一次确认高危特征词已消失。
6. **尊重「不要运行破坏」约束**：只删数据/提示/选项，功能逻辑（导入、渲染、生成框架）一律不动。

## 五、仍存在的风险（提醒）

本次清的是**生成指令与组织层**，以及**个别明确性化的现代诗**。语料库中仍有成规模、带很多判断的**性化儿童**内容模块（如「蒙学艳典/弟子规艳典/三字经艳典/论语艳典」「童话改编」「情色童话」「儿歌童谣」等），这些尚未处理。

- 若仓库要公开，建议进一步清理或把这些模块 `.gitignore` 掉/放私库；
- 即使清理完毕，基于本项目性质，公开仓库仍应配内容警告（README 中声明仅限 18+、禁止未成年与未同意内容），或直接设为**私有**。

## 六、与 Git 相关的其它清理

- 移除 4 个盗版电子书（安徒生/格林/伊索/拉封丹，均带 z-library 标识）——版权/DMCA 风险；
- 删除运行时用户数据（`保存/` 867MB、`日志`、`crash-logs`）——体积与隐私；
- 新增 `.gitignore`（node_modules、dist、`*.bak_*`、`_备份*`、`保存/` 等）；
- 新增 `README.md`（含成人内容警告与使用范围声明）；
- 超大文件 `renderer/数据库/淫诗艳曲/📖 淫诗/淫诗.csv`(170MB) 超过 GitHub 100MB 硬上限，已用 **Git LFS** 管理；
- 仓库待上传唯一内容约 488MB。
