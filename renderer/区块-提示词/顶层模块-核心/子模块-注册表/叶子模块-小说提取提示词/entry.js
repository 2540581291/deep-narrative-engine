// 深度-叙事引擎 · 小说提取提示词
// 小说提取模块的全部 LLM 提示词模板，字段通过 {字段Schema} 占位符注入（由 fields.js 的 小说提取字段Schema 生成）

// ===== AI 决定搜索关键词（针对性提取 Tab2 第一阶段） =====
registerPrompt('ext_char_keywords', {
  system: '你擅长分析小说内容并生成有效的搜索关键词。输出严格 JSON。',
  user: '用户需求：{需求}{性别提示}\n\n以下是小说的第一段正文（约 12 万字），请根据用户需求分析应该搜索哪些关键词来定位相关角色。\n\n输出 3-8 个搜索关键词（可以是人名、称呼、特征词、相关描述词等），以便在全文搜索中找到相关段落来提取角色。\n\n{文本}\n\n输出严格 JSON：{"keywords":["关键词1","关键词2","关键词3"]}',
});

// ===== 分段提取（主流程 + 断点续传共用） =====
registerPrompt('ext_char_extract', {
  system: '你擅长从小说文本中识别所有人物，包括只被提及一次的亲属关系人。输出严格 JSON。优先确保人物识别完整，字段可空但人物不可漏。',
  user: '当前已识别角色（前段已提取结果，避免重复或遗漏）：\n{已识别摘要}\n\n这是长篇小说中的一段文本（{chunkLabel}）。你的任务是提取其中出现或**被提及**的所有人物。\n\n⚠️ 首要任务：找出本段中出现的**所有人**，包括只被提及一次的背景关系人（如张三的父亲、李四的原配、只提了一笔的旧友、管家、侍卫等）。**只要原文提到了某个人，无论戏份多少，都必须提取**。其次再为每个人提取能找到的详细信息。越详细越好。\n\n文本内容：\n{chunkText}\n\n请严格按以下 JSON 格式输出（不要其他文字）：\n注意：aliases 为同一角色的其他称呼（昵称/称号/代称），不包括其他独立角色的名字。无别名则空数组[]。\n{"characters":[\n  {\n  {字段Schema}\n  }\n]}\n注意：\n- **gender 判定**：有女性化表现的男性归 femboy（伪娘），有阴道且有阴茎归 futa（扶她），标准女性归 female，标准男性归 male，动物/怪物/非人生物归 beast（异种）\n- **所有文本字段尽可能详细**，原文怎么描述就怎么提取，不要缩写\n- 未在原文中提及的字段填 null，**不要编造**\n- **只要原文提到某个人名或称呼（包括亲属关系如"他父亲""她姐姐"），都必须提取**，无论戏份多少\n- 只列出本段文字中真正出现或提及的人物（前段已识别角色仅作参考，不要照搬）\n- 不知道真名的用文中称呼（如红衣女子/班主任）\n- **role 判定**：有一定戏份或推动剧情作用给主配及以上，纯背景角色归配角/龙套\n- **sexualDetails 每条不要标注「（第X章）」「（第X段）」等来源信息**\n- **JSON 字符串字段内部不要使用英文双引号**：如需引用词语或台词，请用中文引号「」，避免破坏 JSON 格式\n- 本段没出现角色则输出 {"characters":[]}',
});

// ===== 针对性提取（多关键词 / 单关键词共用） =====
registerPrompt('ext_char_targeted', {
  system: '你擅长从小说文本中{性别筛选}提取人物信息。输出严格 JSON，字段尽可能详细。',
  user: '以下是从小说全文中搜索{关键词}得到的 {段落数} 处相关段落。\n\n{段落}\n\n请从以上段落中提取所有出现或提及的人物。输出严格按以下 JSON 格式（不要其他文字），字段越详细越好：\n{"characters":[\n  {\n  {字段Schema}\n  }\n]}\n\n注意：\n{性别筛选}\n- **gender 判定**：有女性化表现的男性归 femboy（伪娘），有阴道且有阴茎归 futa（扶她），标准女性归 female，标准男性归 male，动物/怪物/非人生物归 beast（异种）\n- **所有文本字段尽可能详细**，原文怎么描述就怎么提取，不要缩写\n- 未在原文中提及的字段填 null，**不要编造**\n- **只要原文提到某个人名或称呼（包括亲属关系如"他父亲""她姐姐"），都必须提取**，无论戏份多少\n- 只列出本段文字中真正出现或提及的人物（前段已识别角色仅作参考，不要照搬）\n- 不知道真名的用文中称呼（如红衣女子/班主任）\n- **role 判定**：有一定戏份或推动剧情作用给主配及以上，纯背景角色归配角/龙套\n- **sexualDetails 每条不要标注「（第X章）」「（第X段）」等来源信息**\n- **JSON 字符串字段内部不要使用英文双引号**：如需引用词语或台词，请用中文引号「」，避免破坏 JSON 格式\n- 本段没出现角色则输出 {"characters":[]}',
});

// ===== 描述生成（批量 / 单个 / 重新提取共用） =====
// {字段Schema} 由调用方传入：从 brief 起的字段定义（name/gender/role 由调用方拼入 {前导}）
registerPrompt('ext_char_desc', {
  system: '你擅长为角色卡生成整理输入素材。输出严格 JSON，内容将作为角色卡生成的起点。性相关字段只在有据可查时填写。',
  user: '以下是从原文各处提取的与【{角色名}】相关的段落，以及分段提取合并后的角色数据。{阶段说明}{用户方向}\n\n请综合所有信息整理成一份完整的角色素材。这份素材将被填入角色卡生成页面，作为生成角色卡的输入起点。\n\n角色在全文中的相关段落：\n{相关段落}\n\n从各段提取中合并得到的【{角色名}】完整数据：{提取数据}\n\n请综合以上所有信息，按以下 JSON 格式输出角色描述（不要其他文字）：\n{前导}{字段Schema}\n}\n\n注意：\n- **以合并提取数据为基准**：输出必须基于「从各段提取中合并得到的完整数据」，合并数据里已有的内容要完整保留、不得删改\n- **用原文段落补充合并数据**：当原文（相关段落）中包含合并数据尚未覆盖、或比合并数据更详细的对应信息时，用原文的信息对相应字段做补充、补全\n- **只补不编**：只有原文确实提供了对应信息才补充；原文和合并数据都没提到的方面填 null，不得自行编造或凭空扩展\n- **sexualDetails（性爱明细）补充规则**：应依据已经提取出的结构化数据（合并提取数据）对性爱明细做补充完善，**不得删除或简写**其中已有内容；即便发现与原文段落不一致，也只能补充、修正细节，**不得删除或简写**已有条目\n- **JSON 字符串字段内部不要使用英文双引号**：如需引用词语或台词，请用中文引号「」，避免破坏 JSON 格式',
});

// ===== 性爱明细生成（仅输出性爱明细，不复写角色详情） =====
registerPrompt('ext_char_sexgen', {
  system: '你擅长从小说文本中提取角色的性爱明细。输出严格 JSON。',
  user: '以下是与角色「{角色名}」相关的原文段落，以及已提取的角色数据。请为该角色生成**性爱明细**（sexualDetails）。\n\n角色数据：{提取数据}\n\n原文相关段落：\n{相关段落}{用户方向}\n\n仅输出以下 JSON（不要其他文字）：\n{"sexualDetailsGen": ["1. ...", "2. ..."]}\n\n注意：\n- **只输出性爱明细数组**，不做任何其他字段\n- 原文描写过的每场性经历/性行为/被调教/被开发的情节都独立成条，按时间顺序编号（1. 2. 3. ...），每条描述详细、内容充足，不能一句话带过\n- 内容需与该角色的性别、种族、身份、性格设定一致\n- 未在原文出现明确性内容时输出空数组 []，**不要凭空编造**\n- 每条不要标注「（第X章）」「（第X段）」等来源信息\n- **JSON 字符串字段内部不要使用英文双引号**：如需引用词语或台词，请用中文引号「」，避免破坏 JSON 格式',
});

// ===== 重新提取（单个角色完整数据） =====
registerPrompt('ext_char_reextract', {
  system: '你擅长从小说文本中提取角色数据。输出严格 JSON，只基于原文内容，不要编造。',
  user: '{范围说明}\n{段落}{用户建议}\n\n输出严格按以下 JSON 格式（不要其他文字），字段越详细越好：\n{\n{字段Schema}\n}\n\n注意：\n- **仅提取这一个角色「{角色名}」**：无论原文中出现多少其他角色、无论其他角色戏份多重，一律不得输出；输出内容只能属于该角色，不要混入其他角色的数据\n- 未在原文中提及的字段填 null，**不要编造**\n- **sexualDetails（性爱明细）务必重视**：原文中出现的**一切性相关内容都要记入**该角色的 sexualDetails 数组——包括每次性行为/性经历（口交、肛交、阴道交、兽交、群交等）、每次被调教/被开发/被刑具使用（穿环、扩肛、灌肠、塞入异物、鞭打性虐等）、每次性相关的情节片段。原文描写过的就逐条提取，独立的事件单独成条，宁全勿缺，不得因为内容少或短而省略\n- **JSON 字符串字段内部不要使用英文双引号**：如需引用词语或台词，请用中文引号「」，避免破坏 JSON 格式\n- 所有文本字段尽可能详细，原文怎么描述就怎么提取，不要缩写\n- **sexualDetails 每条不要标注「（第X章）」「（第X段）」等来源信息**',
});

// ===== 多块提取结果合并（全文提取用：各块提取同一角色，合并去重成完整版） =====
registerPrompt('ext_char_merge', {
  system: '你擅长合并小说角色数据。给定同一角色在多段文本中的多次提取结果，输出合并去重后的完整数据。你必须忠实于已有数据：只能合并、去重、接续，绝不能新增、丢弃、改写或编造任何内容。',
  user: '角色「{角色名}」在全文被分为多段，每段都提取出了一份该角色的数据。请把各段的提取结果**合并去重**成一份完整的角色数据。\n\n⚠️ 最高原则：**你是合并者，不是创作者**。所有内容只能来自{分段数据}中的提取结果，绝不能编造、绝不能丢弃、绝不能改写原文案。\n\n【数组字段（口头禅 catchphrases、做爱说过的话 bedroomTalk、性爱明细 sexualDetails、别名 aliases）——硬性规则】\n- **必须逐条保留所有段中出现的每一条**，一条都不能丢\n- **禁止新增**：不得添加任何不在{分段数据}里的条目\n- **禁止改写**：条目必须原样保留原文案（含语气、用词），不得润色、缩写、合并成新话\n- 只有完全重复（逐字相同）的条目才去重\n- sexualDetails（性爱明细）：**按时间顺序把各段的明细全部接续起来**，然后从 1 开始统一重新编号（1、2、3……连续递增）；若不同段描述了**同一场事件**（可判断为同一次性经历），则把各段对该事件的所有描述内容**合并到同一条里**（各段的细节互补、完整成一条，不要只留其中一段的），合并后仍只算一条；除此之外每条都保留，不得丢弃、不得新增；**每条不要标注「（第X章）」「（第X段）」等来源信息**\n\n【文本字段（外貌、性格、背景、人生经历、性特征、性经验、性偏好等）】\n- 同一信息重复时，合并细节，不重复堆砌\n- 不同段提供的时段变化（如"前期黑发，后期白发"）**必须显式保留**，写成"前期X，后期Y"的形式，不得只留其中一个时段\n- 各段都未提及的字段填 null 或空数组，不要编造\n\n【其他】\n- **JSON 字符串字段内部不要使用英文双引号**：如需引用词语或台词，请用中文引号「」\n\n{分段数据}\n\n输出合并后的完整 JSON（不要其他文字）：\n{\n{字段Schema}\n}',
});

// ===== 时间线评估 =====
// {阶段Schema} 由调用方传入完整阶段对象示例（含 name/description/fields）
registerPrompt('ext_char_timeline', {
  system: '你擅长从全文各段落中综合分析角色的生命历程阶段变化。输出严格 JSON。',
  user: '你擅长分析小说角色的人生阶段变化。\n\n角色「{角色名}」的基本信息：\n{角色信息}\n\n{hintSection}以下是在原小说全文搜索该角色名找到的所有相关段落（均匀分布于全文各处）：\n\n{段落}\n\n请根据以上所有段落，分析该角色在小说中有哪些明显的人生阶段（如有）。\n注意：请通盘考虑所有段落，不要只依赖开头或结尾的信息。一个阶段跨越多个段落是正常的。\n{阶段数}\n返回严格 JSON（不要其他文字）。注意：JSON 字符串字段内部不要使用英文双引号，如需引用词语或台词请用中文引号「」，避免破坏 JSON 格式：\n{\n  "hasStages": true/false,\n  "stages": [\n    {阶段Schema}\n  ]\n}',
});

// ===== 质量审查（性别判定） =====
registerPrompt('ext_char_review', {
  system: '你擅长角色性别分析。只基于已提取数据做判断，重点关注 gender 字段是否与称号、描述、角色名矛盾。输出严格 JSON。',
  user: '以下是从小说中提取的角色列表（按性别排序），请检查每个角色以下维度：性别判定、角色定位、别名规范。\n\n{角色列表}\n\n检查规则一（性别判定）：gender（female/male/femboy/futa/beast）必须严格按照以下规则判定，不得因剧情类型或小说背景而豁免：{性别筛选}\n- 「公主」「少女」「女」等描述 → 应归 female\n- 「王子」「少年」「男」等描述 → 应归 male\n- 「有女性化表现的男性」「伪娘」「女装」→ 应归 femboy\n- 「有阴道且有阴茎」→ 应归 futa（扶她）\n- **动物、怪物、非人生物、魔物、触手等 → 应归 beast（异种）**\n- 性别为男性，但有被操的记录 → 强制认定为伪娘（femboy），不考虑 BL/耽美等剧情因素\n\n检查规则二（角色定位）：根据角色名在原文中的出现频次（freq/rank）按以下排名区间判定角色级别：\n- 排名 1-5 → 角色级别应为「主角」\n- 排名 6-20 → 角色级别应为「主配」\n- 排名 21-50 → 角色级别应为「配角」\n- 排名 51+ → 角色级别应为「龙套」\n- 综合 storyRole、lifeStory、relationships 等字段验证排名是否合理\n- 如排名与当前 role 明显不匹配，说明角色级别判定错误，应当修正\n- 角色名、称号、外貌描写、他人称呼中隐含的性别信息都可作为判断依据\n- 如果有多个角色名指向同一人，合并到审核中{审查方向}\n\n检查规则三（别名规范）：检查每个角色的 aliases 是否为标准数组格式，每个别名应独立为数组元素（如["黛玉","主人"]）。若发现别名中包含中文顿号、逗号等分隔符将多个名称合并写在同一元素中（如["黛玉、主人"]），视为格式违规，需在 corrections 中标注修正\n\n输出严格 JSON：\n{"corrections":[{"type":"fix","name":"角色名","field":"gender/role/aliases","value":"建议的gender/role或aliases正确格式","problem":"问题描述（如：称号为公主却标记为male / 频次排名第1但标记为配角 / aliases含顿号未拆分）","suggestion":"修正建议"}]}\n注意：字符串字段内（problem/suggestion/value 等）不要使用英文双引号，如需引用词语请用中文引号「」，避免破坏 JSON 格式。\n如无任何问题，corrections 返回空数组。',
});

// ===== 一致性审查（合并检测/性别矛盾/角色定位/数据矛盾） =====
registerPrompt('ext_char_review_simple', {
  system: '你擅长角色数据分析与一致性校验。重点关注同名/相似名合并检测。只基于已提取数据做判断。',
  user: '以下是从小说中提取的角色列表（按性别排序），请做详细的一致性检查：\n\n{角色列表}{审查方向}{性别筛选}\n\n检查维度（按优先级）：\n1. 【合并检测】是否有两个角色名指向同一人？例如：全名vs昵称、名字vs称号、高度相似且role相同等\n2. 【性别矛盾】gender是否明显与称号/描述矛盾（如称号"公主"却标记为male）\n3. 【角色定位】role（主角/主配/配角/龙套）是否与剧情占比相符\n4. 【数据矛盾】年龄、种族、职业等字段是否有明显矛盾\n\n输出严格 JSON 格式：\n{"corrections":[{"type":"merge/fix","name":"角色名","mergeInto":"合并目标角色名（type为merge时）","field":"字段名（type为fix时）","value":"建议值（type为fix时）","problem":"问题描述","suggestion":"修正建议"}]}\n注意：字符串字段内（problem/suggestion/value 等）不要使用英文双引号，如需引用词语请用中文引号「」，避免破坏 JSON 格式。\n如无任何问题，corrections 返回空数组。',
});

// ===== 别名搜索 =====
registerPrompt('ext_char_alias', {
  system: '你擅长从小说文本中识别角色的各种称呼。只提取原文中确实出现的称呼，不要编造。',
  user: '在这段小说文本中，角色【{角色名}】出现了吗？{提示}\n- 如果出现了，请列出这个角色在本段中使用或提及的所有其他称呼（昵称、称号、化名、他人对它的称呼等）\n- 如果没出现，aliases 返回空数组\n\n文本内容：\n{文本}\n\n输出 JSON（称呼字符串内不要使用英文双引号）：\n{"found": true/false, "aliases": ["称呼1", "称呼2"]}',
});

// ===== 按用户意见重新审查 =====
registerPrompt('ext_char_review_revise', {
  system: '你擅长角色数据分析。根据用户意见重新评估修正方案。',
  user: '以下是角色提取数据，我已经做了一轮审查并给出了修正方案。现在用户看了之后给出了修改意见，请根据用户的意见重新审查并给出新的修正方案。\n\n角色列表：\n{角色列表}\n\n{旧方案}\n\n用户的修改意见：\n{用户意见}\n\n请根据用户的意见调整修正方案。如认为没有需要修正的，corrections 返回空数组。',
});

// ===== AI 提取书名（重命名记录） =====
registerPrompt('ext_char_rename', {
  system: '你擅长从小说开头提取正式书名。输出格式：《书名》。',
  user: '以下是一部小说的开头部分，请从中提取这部作品的正式书名。原始文件名为「{文件名}」{用户建议}，如果开头明确提到了书名则使用原名，否则根据内容推断。输出格式：《书名》，不要其他文字。\n\n{文本}',
});

// ===== AI 分类 =====
// 补充标签为非必选项：AI 判断不出作品归属时返回 null，不要猜测
registerPrompt('ext_char_classify', {
  system: '你擅长小说分类。输出 JSON 格式：{"type":"正经/色情","origin":"同人/原创","tags":["作品标签，非必选，可为 null"]}',
  user: '小说标题：{标题}\n\n小说开头：\n{开头}\n\n请判断这部小说的分类。\n1. 内容类型：是「正经」还是「色情」？（必选）\n2. 来源类型：是「同人」还是「原创」？（必选）\n3. 补充标签（非必选）：若该小说属于以下典型同人作品之一，在 tags 中填入作品名（不含书名号）：我真没想重生啊、Fate、生肖守护神、斗罗大陆、斗破苍穹、诡秘之主。不属于任何一部则 tags 返回 null，不要猜测。\n\n只输出 JSON，不要解释。',
});
